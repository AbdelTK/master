apiVersion: v1
kind: Service
metadata:
  name: kibana
spec:
  type: {{ .Values.kibana.service.type }}
  selector:
    app: kibana
  ports:
    - protocol: TCP
      port: {{ .Values.kibana.service.port }}
      targetPort: 5601
