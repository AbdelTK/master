apiVersion: v1
kind: Service
metadata:
  name: logstash
spec:
  type: {{ .Values.logstash.service.type }}
  selector:
    app: logstash
  ports:
    - protocol: TCP
      port: {{ .Values.logstash.service.port }}
      targetPort: 5044
